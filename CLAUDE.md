# PhotoTake — instrucciones para Claude

## Rama de trabajo
Desarrollar y hacer push **siempre en `main`**. No usar ramas de feature salvo indicación explícita del usuario.

## Versión
La versión se muestra en `index.html` línea 473:
```html
<span class="ver">vX.Y</span>
```
**Incrementar la versión en cada commit a `main`** (patch: X.Y → X.Y+1).
Versión actual: v2.3
